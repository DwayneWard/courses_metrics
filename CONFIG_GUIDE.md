# Инструкция по настройке конфигураций курса

`CourseAnalytics` умеет автоматически распознавать типы активностей по встроенному классификатору. В конфиге курса вы указываете только путь к ведомости и (при необходимости) уточняете правила группировки/агрегации. Этот документ объясняет, какие параметры доступны.

## Минимальный пример

```python
COURSE_CONFIG = {
    "excel_path": "⚫️ Разработка на Python. Профессиональный.xlsx",
    "stop_before": "Проект 4",
    "activity_rules": []
}
```

Этого достаточно, чтобы инструмент распознал проекты, коллоквиумы, контрольные, домашние задания, контесты и бонусы по дефолтным паттернам.

## Встроенный классификатор типов

| Тип | Роль | Учитывается во вовлечённости | Ключевые слова (без регистра) |
|-----|------|------------------------------|--------------------------------|
| `project` | основная | да | `проект`, `project`, `capstone` |
| `colloquium` | основная | да | `коллоквиум`, `colloquium` |
| `control` | основная | да | `контрольная работа`, `контрольные`, `контрольная точка` |
| `contest` | основная | да | `контест`, `соревнование`, `contest`, `олимпиада` |
| `homework` | основная | нет | `домашнее задание`, `дз`, `homework` |
| `bonus` | бонусная | нет | `бонусная активность`, `бонус`, `bonus` |
| `other` | прочее | нет | — |

Если название столбца (первая или вторая строка ведомости) содержит одну из подстрок, активность получает соответствующий тип. При совпадении нескольких типов выигрывает тот, у которого выше приоритет (например, `bonus` перекрывает `project`).

## Поля конфигурации

| Ключ | Обязателен | Значение | Назначение |
|------|------------|----------|------------|
| `excel_path` | да | строка | Путь к Excel-ведомости. |
| `stop_before` | нет | строка или `None` | Игнорировать все столбцы, начиная с указанного названия. Удобно, если ведомость “растёт” во время семестра. |
| `positive_scores_only` | нет | `True`/`False` | Если `True`, в метриках участвуют только баллы > 0. |
| `ignore_patterns` | нет | список строк | Столбцы, содержащие эти подстроки (в любой строке заголовка), будут пропущены. |
| `activity_rules` | нет | список словарей | Группировка и агрегация столбцов вручную. См. раздел ниже. |
| `activity_types` | нет | словарь | Переопределение дефолтного классификатора (добавление паттернов, смена роли, приоритета и т.д.). |
| `primary_activity_types` | нет | список | Явный список “основных” типов для отчётов. По умолчанию — все типы с ролью `primary`. |
| `bonus_types` | нет | список | Типы, которые считаются бонусными. По умолчанию — все с ролью `bonus`. |
| `engagement_activity_types` | нет | список | Типы, попадающие в траекторию вовлечённости. По умолчанию — все типы, у которых флаг `engagement = True`. |
| `type_order` | нет | список | Необязательный порядок для отображения типов в таблицах. Без указания строится из основных → бонусных → остальных. |

### Настройка `activity_types`

Каждая запись — словарь с ключами, которые переопределяют дефолт:

```python
"activity_types": {
    "homework": {
        "patterns": ["лабораторная"],  # Добавить паттерн
        "role": "primary",             # Изменить роль (если нужно)
        "engagement": True,              # Включить в траекторию вовлечённости
        "priority": 65                   # Изменить приоритет (для конфликтов)
    },
    "attendance": {
        "label": "Посещаемость",
        "patterns": ["посещаемость"],
        "role": "other",
        "engagement": False,
        "priority": 40
    }
}
```

Если указать новый тип (например, `attendance`), он будет добавлен к классификатору и его можно использовать в правилах и метриках.

## Правила активностей (`activity_rules`)

Используйте правила, чтобы объединять несколько столбцов в одну активность, задавать агрегирование или жестко назначать тип.

```python
{
    "name": "Проект 1",                # Название активности в отчётах
    "match": ["🏆 Проект. Часть 1", "🏆 Проект. Часть 2"],
    "type": "project",                 # Любой тип из классификатора или свой
    "aggregation": "sum",              # См. таблицу агрегаций ниже
    "original_name": "Проект 1"        # (опционально) оригинальное название
}
```

Поддерживаются поля `match` / `matches` / `includes` / `patterns` — укажите любую комбинацию подстрок без учёта регистра. Если `type` опущен, будет использован результат авто-классификации.

### Доступные агрегации

| Значение | Поведение |
|----------|-----------|
| `single` / отсутствует | Берётся первый подходящий столбец. |
| `sum` | Сумма всех столбцов активности. |
| `mean` | Среднее значение. |
| `max` / `min` | Максимум / минимум. |
| `last` | Последний столбец (например, последняя попытка). |

## Пример продвинутой конфигурации

```python
COURSE_CONFIG = {
    "excel_path": "Основы разработки на Go.xlsx",
    "stop_before": None,
    "ignore_patterns": ["итого"],
    "activity_types": {
        "homework": {"patterns": ["домашнее задание", "домашние задания"]},
        "control": {"patterns": ["контрольные работы"]},
        "bonus": {"patterns": ["бонусная активность"]}
    },
    "activity_rules": [
        {
            "name": "Неделя 7. Бонус",
            "match": ["Неделя 7."],
            "type": "bonus"
        }
    ]
}
```

## Проверка конфигурации

1. Создайте анализатор:
   ```python
   analytics = CourseAnalytics(config=COURSE_CONFIG)
   ```
2. Посмотрите маппинг столбцов:
   ```python
   analytics.describe_sources()
   ```
   Таблица покажет, какие колонки попали в каждую активность, их тип и выбранную агрегацию.
3. Если колонка не распознана:
   - добавьте паттерн в `activity_types`;
   - создайте явное правило в `activity_rules`;
   - проверьте, не попадает ли столбец под `ignore_patterns`.

## Частые сценарии

- **Несколько частей одного проекта** — объедините их правилом с `aggregation = "sum"` или `"max"`.
- **Своя терминология** — добавьте подстроки в `activity_types` для соответствующего типа.
- **Нужно исключить служебные столбцы** — укажите их в `ignore_patterns`.
- **Нет проектов (или других типов)** — просто не используйте их; метрики автоматически опираются на те типы, что доступны после классификации.

При необходимости расширяйте конфигурацию: словарь допускает дополнительные ключи, которые вы можете обработать в собственной логике поверх `CourseAnalytics`.

